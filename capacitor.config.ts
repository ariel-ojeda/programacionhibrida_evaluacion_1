import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'evaluacion.ariel.ojeda',
  appName: 'evaluacion-ariel-ojeda',
  webDir: 'www'
};

export default config;
